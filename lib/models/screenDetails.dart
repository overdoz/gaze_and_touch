import 'package:flutter/material.dart';

class ScreenSize {
  double height;
  double width;

  ScreenSize(this.height, this.width);
}

class ScreenData {
  double height;
  double width;
  Offset position;

  ScreenData(this.height, this.width, this.position);
}
